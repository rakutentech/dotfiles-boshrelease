#!/bin/bash
# Environment variables those templated from properties

SHRC_PROMPT_PREFIX='<%= "[#{spec.deployment} #{name}/#{index} #{spec.networks.to_h.values.first.ip}] " %>'
SHRC_PROMPT_COLOR="<%= p('shrc.prompt_color') %>"
SHRC_CWD_COLOR=light_blue
